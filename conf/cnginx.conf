
worker_process      1;

events {
    use epoll;
}

http {
    server {
        server_name www.cnginx.com;
        listen      80;

        location / {
            root html;
        }

        location /test {
            root html/testdir;
        }
    }
}
